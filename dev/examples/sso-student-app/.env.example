# Hydra SSO Configuration
HYDRA_BASE_URL=https://hydra.newpaltz.edu
APP_NAME=My Student App
PORT=5175

# Roles allowed into /restricted (comma-separated)
ALLOWED_ROLES=student,faculty

# Optional: force a base path (normally auto-detected from X-Forwarded-Prefix)
# APP_BASE_PATH=/students/yourname/yourproject/
