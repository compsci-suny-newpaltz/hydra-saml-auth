apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hydra-infra
resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - middleware.yaml
  - ingressroute.yaml
  - webhook.yaml
  - cronjob-backup.yaml
# secret.yaml is applied by hand (see secret.yaml.example) and gitignored.
